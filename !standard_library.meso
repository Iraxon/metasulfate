{
    '+= = ['x 'y] =|> (x = x + y);
    '-= = ['x 'y] =|> (x = x - y);
    '*= = ['x 'y] =|> (x = x * y);
    '/= = ['x 'y] =|> (x = x / y);
    `**2 = ['x] =|> [x * x];

    'sin = HARDCODE!SIN;
    'cos = HARDCODE!COS;
    'tan = HARDCODE!TAN;

    'print = HARDCODE!PRINT;
}
